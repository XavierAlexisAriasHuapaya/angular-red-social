import { ChatNotificationDirective } from './chat-notification.directive';

describe('ChatNotificationDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatNotificationDirective();
    expect(directive).toBeTruthy();
  });
});
